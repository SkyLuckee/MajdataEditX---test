<Window x:Class="MajdataEdit.ConnectShare"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:local="clr-namespace:MajdataEdit"
        xmlns:properties="clr-namespace:MajdataEdit.Properties" 
        lex:LocalizeDictionary.DesignCulture="zh-CN"
        lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        mc:Ignorable="d"
        Background="{DynamicResource WindowBackground}"
        Title="{lex:Loc Key=ConnectChartShare}" 
        Height="160" Width="200" ResizeMode="NoResize">
    <Grid Margin="5,5,5,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="100*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
            </Style>
            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="Height" Value="26" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="Height" Value="26" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="Background" Value="{DynamicResource ButtonsBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
            </Style>
        </Grid.Resources>

        <Label Grid.Row="0" Grid.Column="0"
               Content="{lex:Loc Key=ConnectIP}" />
        <TextBox x:Name="ConnectIP"
                 Grid.Row="0" Grid.Column="1"
                 Text="{Binding Source={x:Static properties:Settings.Default}, 
                                Path=LastServerAddress, 
                                Mode=TwoWay, 
                                UpdateSourceTrigger=PropertyChanged}" />
        
        <Label Grid.Row="1" Grid.Column="0"
               Content="{lex:Loc Key=ConnectPort}" />
        <TextBox x:Name="ConnectPort"
                 Grid.Row="1" Grid.Column="1"
                 Text="{Binding Source={x:Static properties:Settings.Default}, 
                                Path=LastServerPort, 
                                Mode=TwoWay, 
                                UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Orientation="Horizontal"
                    Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center">
            <Button x:Name="Save_Button" Content="{lex:Loc Key=Connect}"
                    Width="70" Height="28" Margin="0,5,10,5"
                    Foreground="{DynamicResource ButtonForeground}"
                    Background="{DynamicResource ButtonsBackground}"
                    Click="Connect_Button_Click" />
            <Button x:Name="Cancel_Button" Content="{lex:Loc Key=Cancel}"
                    Width="70" Height="28" Margin="10,5,0,5"
                    Foreground="{DynamicResource ButtonForeground}"
                    Background="{DynamicResource ButtonsBackground}"
                    Click="Cancel_Button_Click" />
        </StackPanel>
    </Grid>
</Window>
